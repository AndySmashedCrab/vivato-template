# Modules

Future modules live under `modules/<module-name>/frontend` and `modules/<module-name>/backend`.

Each module is a full-stack feature unit and is registered explicitly by the host applications in v1.
